apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: daprmq-blobstore
spec:
  type: bindings.localstorage
  version: v1
  metadata:
    - name: rootPath
      value: "/blobstore"
scopes:
  - daprmq-service
