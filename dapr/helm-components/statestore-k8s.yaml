apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.postgresql
  version: v2
  metadata:
    # Connection string
    - name: connectionString
      value: "host=my-release-postgresql.default.svc.cluster.local user=postgres password=tSIhveB1Mm port=5432 connect_timeout=10"
    - name: actorStateStore
      value: "true"
    - name: tablePrefix
      value: daprmq_
